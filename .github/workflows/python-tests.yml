name: Python tests

on:
  push:
    branches:
      - main
      - master
      - develop
      - add-ingest-manifest
  pull_request:
    branches:
      - main
      - master
      - develop

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install test dependencies
        run: |
          python -m pip install --upgrade pip
          pip install pytest requests python-dotenv google-cloud-storage

      - name: Run tests
        run: pytest -q
